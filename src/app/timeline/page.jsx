import TimeLineCart from "@/components/MetaData/TimeLine";

export const metadata = {
  title: 'All Timeline',
  description: 'all the users timeline info',
}

const Timeline = () => {
    return (
        <div>
            <TimeLineCart></TimeLineCart>
        </div>
    );
};

export default Timeline;