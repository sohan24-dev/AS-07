import StaticPage from "@/components/MetaData/Static";
export const metadata = {
  title: 'Static Page',
  description: 'all the users timeline info',
}

const Static = () => {
    return (
        <div>
            <StaticPage></StaticPage>
        </div>
    );
};

export default Static;